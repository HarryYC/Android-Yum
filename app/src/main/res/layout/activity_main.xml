<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <RelativeLayout
        android:id="@+id/a1"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        tools:context=".MainActivity">

        <ImageButton
            android:id="@+id/shakeIB"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentStart="true"
            android:layout_below="@+id/searching_edit_text"
            android:layout_gravity="center"
            android:layout_marginTop="36dp"
            android:adjustViewBounds="true"
            android:clickable="false"
            android:padding="0dp"
            android:scaleType="fitCenter"
            android:src="@mipmap/test" />

        <EditText
            android:id="@+id/searching_edit_text"
            android:layout_width="280dp"
            android:layout_height="50dp"
            android:layout_alignParentTop="true"
            android:layout_centerHorizontal="true"
            android:layout_marginTop="20dp"
            android:background="@drawable/edit_text_style"
            android:hint="   Keyword for Shaking"
            android:imeOptions="actionSearch"
            android:inputType="text" />

        <Button
            android:id="@+id/filter_button"
            style="?android:attr/buttonStyleSmall"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignBottom="@+id/searching_edit_text"
            android:layout_alignEnd="@+id/searching_edit_text"
            android:onClick="onFilterClick"
            android:text="Filter" />


    </RelativeLayout>

    <fragment
        android:id="@+id/fragment_navigation_drawer_fragment"
        android:name="app.team3.t3.tabs.NavigationDrawerFragment"
        android:layout_width="280dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        app:layout="@layout/fragment_navigation_drawer"
        tools:layout="@layout/fragment_navigation_drawer" />
</android.support.v4.widget.DrawerLayout>